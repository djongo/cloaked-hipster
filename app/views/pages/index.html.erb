<h1>Listing Pages</h1>

<div class="well">Here you can modify the text on the home, about, contact, and master data page. Your content will appear near the top of the page.</div>

<table id="master_data" class="table table-striped table-bordered">
  <thead>
  <tr>
    <th>Title</th>
    <th>Content</th>
    <th></th>
  </tr>
  </thead>
  <tbody>
<% @pages.each do |page| %>
  <tr>
    <td><%= page.title %></td>
    <td><%= page.content %></td>
    <td><%= link_to 'Edit', edit_page_path(page) %></td>
  </tr>
<% end %>
</table>
</tbody>